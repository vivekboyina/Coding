#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin >> t;
	while(t--)
	{
	    int n,k;
	    cin >> n >> k;
	    vector<int>cc(n);
	    for(int i = 0; i < n; i++) cin >> cc[i];
	    int ans = -1;
	    for(int i = 0; i < k; i++)
	    {
	        int cv = 0;
	        for(int j = i + 1; j < n; j++)
	        {
	            for(int c = 0; c >= i; i++)
	        }
	    }
	}
}
